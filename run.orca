using krill;
using time;

if argv.size() == 0 {
	port = 6379;
}
else {
	port = argv[0].integer();
}

print: 'start krill server on port %d' % port;

parallel krill.server(port);
time.sleep(1);
krill.graphite.server(2003);

krill.server(port);
